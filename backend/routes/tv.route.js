import express from 'express';
// import { somthing } from "../controllers/tv.controller.js";

/**
 * Express Router instance for handling tv-show routes.
 */
const router = express.Router();

/**
 * Export the Express Router instance for tv-show routes.
 */
export default router;
